export type CreateBookRequest = {
  booksName: string;
  categoryId: string;
  bookSourceId: string;
};

export type CreateBookSoureceRequest = {
  name: string;
};
export type CreateBookCategoryRequest = {
  name: string;
};
